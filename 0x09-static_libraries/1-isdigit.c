#include <stdio.h>
#include "main.h"

/**
 * _isdigit - checks if a character is a digit
 * @c: the character
 *
 * Return: 1 if true and 0 if false
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

