#include <stdio.h>
#include "main.h"

/**
 * _abs - writes the absolute value of an integer
 * @n: the integer
 * Return: the absolute value of an integer
 */
int _abs(int n)
{
	return (n < 0 ? -n : n);
}
