#include "main.h"
/**
 * _strspn - calculates the lenght of the
 * initial segment of s which consists of bytes
 * @s: string
 * @accept: accept
 *
 * Return: success 1
 */
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}

